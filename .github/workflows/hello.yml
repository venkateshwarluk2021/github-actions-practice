name:  Say Hello

on:
  #push:
  workflow_dispatch:
    branches:
      - main

jobs:
  say-hello:
    runs-on:  ubuntu-latest
    steps:
      - name: Print a greeting
        run:  echo "Hello from  Github Actions"
